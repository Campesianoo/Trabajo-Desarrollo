info:
  name: POST profesor-especialidad
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:3000/api/profesores/46537620/especialidades
  body:
    type: json
    data: |-
      {
        "idEspecialidad": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
